# v1.0 readiness checklist

**Date drafted:** 2026-05-27
**Target tag date:** 2026-06-16
**Claim boundary for v1.0:** *first stable public developer release*. Not "GA", not "production-ready", not "production-hardened", not "battle-tested", not "enterprise-ready", not "hardened".

This is the single artifact to sign off before authorizing the v1.0 tag. Each line below is the answer to one of the seven explicit go/no-go questions from the v0.9.4 plan approval, with the source of truth that backs it. **No item below relies on a private repo or a promise** — every claim is reproducible from public artifacts.

---

## 1. Is the quickstart reliable?

✅ **Yes — verified end-to-end against the published `:latest` image at SHA `fe82b9b`.**

| Claim | Evidence |
|---|---|
| Fresh `docker compose pull && up -d` against an empty volume brings `/readyz` to `ready` within 30s | [v0.9.4 smoke scenario 1](v0.9.4-smoke.md) (1s on test host) |
| Returning-user `docker compose pull && up -d` from a stale image refreshes to current | [v0.9.4 smoke scenario 2](v0.9.4-smoke.md) (0.9.0 → 0.9.2) |
| `getting-started.md` 5-minute path runs without copy-pasta surprises | [statewave-docs#50](https://github.com/smaramwbc/statewave-docs/pull/50) + [#52](https://github.com/smaramwbc/statewave-docs/pull/52) |
| `minimal-quickstart/quickstart.py` runs end-to-end against the smoke server | [v0.9.4 smoke scenario 5](v0.9.4-smoke.md) (5 memories, 114 tokens, full provenance) |

**No blocker.**

---

## 2. Are the SDKs usable enough?

✅ **Yes for the default v1.0 demo path; one fast-follow gap is tracked and dated.**

| Claim | Evidence |
|---|---|
| Python SDK `0.10.2` happy path works (ingest → compile → context → delete) | [v0.9.4 smoke scenario 3](v0.9.4-smoke.md) |
| TypeScript SDK `0.10.2` happy path works | [v0.9.4 smoke scenario 4](v0.9.4-smoke.md) |
| Receipts (`verify`/`replay`) reachable from both SDKs | [statewave-py 0.10.1](https://pypi.org/project/statewave/0.10.1/) + [@statewavedev/sdk 0.10.1](https://www.npmjs.com/package/@statewavedev/sdk/v/0.10.1) |
| `session_id` on `create_episode` works in both SDKs | [statewave-py#18](https://github.com/smaramwbc/statewave-py/pull/18) + [statewave-ts#19](https://github.com/smaramwbc/statewave-ts/pull/19) (both v0.10.2; closes [statewave#174](https://github.com/smaramwbc/statewave/issues/174)) |
| Framework quickstarts (`langchain`, `crewai`, `autogen`) install cleanly with the documented pins | [statewave-examples#15](https://github.com/smaramwbc/statewave-examples/pull/15) |

**One known gap, tracked and explicitly dated post-v1.0:**

- [statewave#176](https://github.com/smaramwbc/statewave/issues/176) — Python SDK governance helpers (`list_suggested_labels`, `promote_suggested_labels`, typed `PolicyDecision` on receipts). Auto-labeling is opt-in (`STATEWAVE_AUTO_LABELING_ENABLED=false` by default), so the default v1.0 user does not hit this. Operators who do enable it have a documented raw-HTTP path. Target: `v0.10.3` post-v1.0 fast-follow.

**No v1.0 blocker.**

---

## 3. Are limitations honest?

✅ **Yes — the published "Still in progress / not yet proven" section in `why-statewave.md` matches the carry-forward risk list in every release-notes file.**

The honest list (carried into v1.0):

- Production-scale not load-tested at >10k subjects / high throughput. Multi-replica API supported and verified since v0.8 (Fly multi-machine + Helm HPA); **single-Postgres only, no cross-region clustering.**
- Multi-tenant isolation is app-layer query scoping; no Postgres RLS yet.
- LLM-compiler vs heuristic-compiler comparative eval not yet published.
- 50-session production-scale benchmark not yet run.
- No second-region deploy verified (residency enforcement is implemented and audited per-region; cross-region failover is not tested because we run one region today).
- No admin-action identity yet (`promoted_by` is null until that layer ships).
- No federated cross-region audit.
- Receipt replay is "current code + original policy" (byte-for-byte historical reproduction needs memory snapshots, deferred).

| Surface | Last verified consistent |
|---|---|
| [`why-statewave.md`](../why-statewave.md) — `## Still in progress / not yet proven` | v0.9.4 audit (this lap) |
| [`statewave/README.md`](https://github.com/smaramwbc/statewave/blob/main/README.md) — `## Honest status` | Reads consistent with `why-statewave.md` post-this-lap |
| [`product.md`](../product.md) — limitations table | v0.9.4 audit (this lap) |
| [`comparisons/mem0.md`](../comparisons/mem0.md) — "What Statewave is not" | v0.9.4 audit (this lap) |
| [v0.9.3 close-out](v0.9.3-launch-readiness.md) — "Risks carried into v0.9.4 / v1.0" | v0.9.4 audit (this lap; wording corrected to match the README's multi-replica claim) |

**No v1.0 blocker.**

---

## 4. Are proof figures consistent across launch surfaces?

✅ **Yes — recomputed from source during v0.9.3 + v0.9.4 and mirrored across every launch surface.**

| Figure | Source command | Value |
|---|---|---|
| Server tests | `pytest --collect-only` | 869 |
| Python SDK tests | `pytest --collect-only` | 77 (post-v0.10.2; was 73) |
| TypeScript SDK tests | `vitest --reporter=verbose` | 50 (post-v0.10.2; was 47) |
| Connector tests | `find packages -name '*.test.ts' \| xargs grep -hcE '^\s*(it\|test)\('` | 606 |
| Eval assertions (support-agent context quality) | `pytest --collect-only eval-support-agent/` | 56 across 23 tests |
| Workflow benchmark (support-agent) | `benchmark-support-agent/results/*.json` | 8/8 Statewave vs 2/8 naive |

Drift risk: the `independent`-tagged SDK version cells in [`architecture/repo-map.md`](../architecture/repo-map.md) are intentionally NOT in the auto-bump path (per `tools/_targets.py`). v0.9.3 caught up two SDK waves of drift; v0.9.4 caught up the v0.10.2 wave. **Going forward, every SDK release must follow [`tools/RELEASE-CHECKLIST.md`](../tools/RELEASE-CHECKLIST.md) or these cells will drift again.**

**No v1.0 blocker.**

---

## 5. Are docs aligned?

✅ **Yes — the v0.9.4 launch-doc-freeze audit caught and closed three real drifts.**

| Drift | Where | Fix |
|---|---|---|
| Subject-scoped admin memory listing missing label fields | [statewave#177](https://github.com/smaramwbc/statewave/pull/177) | Code fix (additive); landed in `:latest` |
| Single-node-only wording contradicting the README's multi-replica claim | 5 surfaces (`product.md`, `why-statewave.md` ×2, `comparisons/mem0.md`, v0.9.3 close-out) | [statewave-docs#56](https://github.com/smaramwbc/statewave-docs/pull/56) |
| ProductPage governance section anchored on v0.8 | `statewave-web/src/pages/ProductPage.tsx` | [statewave-web#106](https://github.com/smaramwbc/statewave-web/pull/106) |
| Repo-map SDK cells stale post-v0.10.2 | `architecture/repo-map.md`, `dev/conventions.md` | [statewave-docs#56](https://github.com/smaramwbc/statewave-docs/pull/56) |

**Forbidden-claim audit:** zero hits for "GA", "production-ready", "production-hardened", "battle-tested", "enterprise-ready", "hardened" as positive claims. The single hit in `why-statewave.md:152` ("Not battle-tested at scale") is a negative/limitation claim and stays.

**No v1.0 blocker.**

---

## 6. Are release/tag steps repeatable?

✅ **Yes — and the v0.9.3 + v0.9.4 laps both exercised the canonical flow.**

| Step | Source |
|---|---|
| Canonical release flow (9 steps) | [`tools/RELEASE-CHECKLIST.md`](../tools/RELEASE-CHECKLIST.md) |
| Server version-mirror auto-bump | `tools/bump-version.py` (gates the server release workflow) |
| Cross-repo version audit (server / contract only) | `tools/check-versions.py` |
| SDK independent-version flag | `tools/_targets.py` (declares SDK rows as `independent`; never asserts equality) |
| Lessons learned from v0.9.2 mirror drift | Embedded in `tools/RELEASE-CHECKLIST.md` |

Both SDKs released `v0.10.2` in this lap following the checklist: PyPI + npm publish workflows are triggered on tag push; both completed successfully today.

**No v1.0 blocker.**

---

## 7. Are there any blockers to a v1.0 tag?

❌ **None observed.**

The v0.9.4 smoke was 11/11 PASS (including 2 bonus checks confirming v0.9.3 work survived into `:latest`). The only material finding (subject-scoped admin listing missing label fields) was landed in-stream as [statewave#177](https://github.com/smaramwbc/statewave/pull/177) and re-validated against the freshly-published image. All three v0.9.4 audit findings (multi-replica wording, ProductPage anchor, repo-map cells) have PRs open and CI green.

| Open thread | Disposition |
|---|---|
| [statewave#174](https://github.com/smaramwbc/statewave/issues/174) — Python SDK `create_episode(session_id=...)` | Closed in v0.10.2 ✅ |
| [statewave#176](https://github.com/smaramwbc/statewave/issues/176) — SDK governance helpers | Deferred to v0.10.3 post-v1.0 fast-follow (target ~2026-07-15). Tracked publicly. Auto-labeling is opt-in; default v1.0 user does not hit it. |
| [statewave#178](https://github.com/smaramwbc/statewave/issues/178) — `GET /v1/version` endpoint | Deferred to v0.10.x. Tracked publicly. Documented workaround. |
| Production-scale load test | Deferred post-v1.0 ("Still in progress / not yet proven" in `why-statewave.md`). |
| RLS, second-region deploy, federated audit, byte-for-byte replay | Deferred post-v1.0. Honest in published limitations. |

---

## Explicit claim boundary for v1.0

When the v1.0 tag is cut, the workspace surfaces SHOULD say:

> **v1.0 — first stable public developer release.**

The workspace surfaces MUST NOT say (per the v0.9.4 plan approval):

- GA
- production-ready
- production-hardened
- battle-tested
- enterprise-ready
- hardened

A v1.0 launch user can expect:

1. The documented quickstart works against the published image.
2. Both SDKs install cleanly from their public registries and the typed surface matches the REST contract.
3. The published "Still in progress / not yet proven" list is honest about what is NOT proven yet.
4. Receipts + sensitivity labels + policy + residency are all reachable from the API and the admin console.
5. Multi-replica API deployment is supported (single-Postgres only).

What a v1.0 launch user MUST NOT expect, and what the public docs say so:

1. Production-scale load testing at >10k subjects.
2. Postgres RLS for tenant isolation.
3. A second region you can fail over to.
4. Admin-action identity (`promoted_by`).
5. Federated cross-region audit.
6. Byte-for-byte historical replay.
7. LLM-compiler-vs-heuristic comparative eval results.

---

## Sign-off rubric

The maintainer signing off on the v1.0 tag confirms:

- [ ] All seven questions above are answered ✅.
- [ ] The four v0.9.4 PRs (statewave#177, statewave-py#18, statewave-ts#19, statewave-docs#56, statewave-web#106) are merged.
- [ ] The v0.9.4 close-out and CHANGELOG entry are merged.
- [ ] No PR opened during the lap was merged red.
- [ ] No surface uses the forbidden-claim wording.
- [ ] No new feature shipped in v0.9.4 outside the agreed in-stream-fix slot.
- [ ] `tools/check-versions.py` passes against the candidate tag.
- [ ] The 16 June launch post drafts (separate, in `statewave-launch`) point at this checklist as the evidence trail.

If any box above is unchecked at tag-cut time, the launch slips. Better honest than performative.
